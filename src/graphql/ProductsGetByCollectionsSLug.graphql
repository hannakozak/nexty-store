query ProductsGetByCollectionSlug($slug: String!, $productsPerPage: Int!, $offset: Int!) {
	collections(where: { slug: $slug }) {
		products(first: $productsPerPage, skip: $offset) {
			...ProductListItem
		}
	}
}
